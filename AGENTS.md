# Log Agent 工作区规则

- 本项目唯一开发根目录是 `D:\日志agent`。后续日志 Agent 的代码、测试和文档都在此目录内修改。
- 不要在原 Codex 临时工作区或其他路径重新创建 `log-agent` 副本。
- Go 模块根目录就是当前目录；运行命令时以此目录作为工作目录。
- `artifacts/` 保存历史方案、截图、Canvas 和中间产物，不参与 Go 服务编译；除非任务明确要求，不要把其中代码接入运行时。
- 开始修改前先阅读 `README.md`、`docs/spec.md` 和当前阶段文档。
- 保持 Eino 与飞书 SDK 只存在于各自适配层，架构边界由测试约束。
- Go 代码修改后运行 `gofmt -w .`、`go test ./...` 和 `go vet ./...`；具备 CGO/C 编译器时补跑 `go test -race ./...`。
- M0 当前使用 Mock SLS；没有真实接入前，不要把固定数据描述为阿里云生产日志结果。
